# Building-a-User-Configuration-Manager
A User Configuration Manager lets users change things like the theme, language, and alerts by adding features that let users view, change, remove, and add settings.
